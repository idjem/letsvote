{
  "name": "letsvote",
  "version": "0.1.0",
  "description": "const Vote = require(\"./vote\");",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mout": "^1.0.0"
  }
}
